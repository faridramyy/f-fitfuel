import 'package:flutter/material.dart';

class AiTrainer extends StatelessWidget {
  const AiTrainer({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('AI Trainer Demo')),
      body: Center(child: Text('Welcome to the AI Trainer Demo Page!')),
    );
  }
}
